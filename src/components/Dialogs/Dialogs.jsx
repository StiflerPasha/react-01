import React from "react";
import classes from "./Dialogs.module.css"

const Dialogs = (props) => {
    return (
        <div>
            <p className={classes.text}>Dialogs</p>
        </div>
    );
};

export default Dialogs;