import React from "react";
import classes from "./News.module.css"

const News = (props) => {
    return (
        <div>
            <p className={classes.text}>News</p>
        </div>
    );
};

export default News;